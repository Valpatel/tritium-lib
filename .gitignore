__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
.venv/
*.egg
.pytest_cache/
.mypy_cache/

# Trained ML models (rebuild with the train CLI; binaries don't belong in git)
data/models/*.pkl
data/models/*.json
.memsearch/
docs/local/

# LLM fleet config — contains private LAN hosts; only the .example is tracked
src/tritium_lib/conf/llm-fleet.conf
src/tritium_lib/conf/ollama-fleet.conf
